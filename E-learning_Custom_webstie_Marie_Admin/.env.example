# Admin frontend (Vite) — baked at Docker/CI build time (not runtime .env on EC2).
# Set GitHub Variables for production builds; local: copy to .env before npm run build.

# Full API base including /api/v1
VITE_API_BASE_URL=https://api.lapropulserie.fr/api/v1

# Optional: CDN / media host for avatars & uploads
VITE_IMAGE_BASE_URL=https://d4k0cugb067ei.cloudfront.net
