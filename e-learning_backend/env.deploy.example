# Copy to EC2 as `.env.deploy` (not committed).
# CI updates API_IMAGE on each deploy.
# Runtime secrets stay in `.env` (Atlas, Stripe, Google, JWT, …) — editable anytime + restart.

API_IMAGE=ghcr.io/lapropulserie/e-learning-api:latest
PORT=8005
