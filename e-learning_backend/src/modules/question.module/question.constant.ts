export enum TQuestionaryCategory {
  free = 'free',
  module = 'module',
  capsule = 'capsule',
}

export enum TQuestionType {
  single = 'Single Select',
  multi = 'Multiple Select',
  textInput = 'Text Input',
  textArea = 'Text Area',
}

export enum TStudentQuestionaryStatus {
  inProgress = 'inProgress',
  completed = 'completed',
}

export enum TStudentAnswerStatus {
  completed = 'completed',
  submitted = 'submitted',
}
