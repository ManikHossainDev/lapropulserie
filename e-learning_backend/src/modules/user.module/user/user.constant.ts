export enum TStatusType {
  active = 'active',
  inactive = 'inactive',
  suspended = 'suspended',
}

export enum TJourneyType {
  individual = 'individual',
  expedition = 'expedition',
  both = 'both',
}

export enum TPreferredLanguage {
  en = 'en',
  bn = 'bn',
}
