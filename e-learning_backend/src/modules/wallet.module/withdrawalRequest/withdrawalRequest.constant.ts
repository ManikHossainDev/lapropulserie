export enum TWithdrawalRequest {
  requested = 'requested',
  processing = 'processing',
  completed = 'completed',
  failed = 'failed',
  rejected = 'rejected',
}

export enum TRequestStatus {
  accept = 'accept',
  reject = 'reject',
}
