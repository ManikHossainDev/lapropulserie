const parentFolder = "e-learning";

export enum TFolderName {
  journey = `${parentFolder}/journey`,
  journeyCapsule = `${parentFolder}/journey-capsule`,
  journeyModule = `${parentFolder}/journey-module`,
  journeyLesson = `${parentFolder}/journey-lesson`,
  individualCapsuleCategory = `${parentFolder}/individual-capsule-category`,
  individualCapsule = `${parentFolder}/individual-capsule`,
  individualModule = `${parentFolder}/individual-module`,
  individualLesson = `${parentFolder}/individual-lesson`,
  user = `${parentFolder}/user`,
  wallet = `${parentFolder}/wallet`,
  common = `${parentFolder}/common`,
  profile = `${parentFolder}/profile`,
}
