# Student / marketing frontend (Next.js)
# Copy to .env.local for local dev. Production API URL is set at Docker
# build time (GitHub Actions variable NEXT_PUBLIC_API_BASE_URL) — rebuild to change.

# Backend origin only — no trailing /api/v1
NEXT_PUBLIC_API_BASE_URL=https://maniknew8080.sobhoy.com
